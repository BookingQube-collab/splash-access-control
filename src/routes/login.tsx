import { StaffLogin } from "@/components/role-login";

export default function LoginPage() {
  return (
    <StaffLogin
      title="Staff sign in"
      subtitle="Sign in with your SummerSplash staff account. You'll be routed to the right portal automatically."
    />
  );
}
